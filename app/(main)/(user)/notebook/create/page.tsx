import CreateNotebook from "@/components/user/notebook/create";

export const metadata = {
  title: "Tạo Notebook mới | Notebooks",
  description: "Tạo notebook cá nhân mới theo cách thủ công hoặc để AI hỗ trợ",
};

export default function CreateNotebookPage() {
  return <CreateNotebook />;
}
